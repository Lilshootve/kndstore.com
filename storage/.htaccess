# Prevent web access to storage - Apache 2.4
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>

# Fallback for Apache 2.2
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
